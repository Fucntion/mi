function homeCtrl($scope,$cookieStore,$routeParams) {
  // console.log($cookieStore.get('usr'));

    // $cookieStore.put('loginStatus',true,{'expires': expireDate});
    // console.log($cookieStore.get('loginStatus'));
//     1:get(key)     获取cookie
//
// 2:put(key,value)   设置cookie
//
// 3:remove(key)    移除cookie



  

}
